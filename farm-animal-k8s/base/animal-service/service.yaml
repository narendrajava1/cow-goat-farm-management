apiVersion: v1
kind: Service

metadata:

  name: animal-service

  namespace: farm-animal

spec:

  selector:

    app: animal-service

  ports:

    - port: 8080

      targetPort: 8080

  type: ClusterIP